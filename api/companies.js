'use strict';
const registry=require('../modules/company-registry/service');
module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');try{if(req.method==='GET'){const id=typeof req.query.id==='string'?req.query.id:'';if(id){const item=await registry.get(id);return item?res.status(200).json({contract:'company.registry.v1',item}):res.status(404).json({error:'COMPANY_NOT_FOUND'});}const search=typeof req.query.search==='string'?req.query.search:'';const limit=Math.min(Number(req.query.limit)||50,100),offset=Math.max(Number(req.query.offset)||0,0);return res.status(200).json({contract:'company.registry.v1',...(await registry.list({search,limit,offset}))});}if(req.method==='POST'){const item=await registry.upsert(req.body||{});return res.status(200).json({contract:'company.registry.v1',item,writes_to_legacy:false});}return res.status(405).json({error:'METHOD_NOT_ALLOWED'});}catch(e){return res.status(400).json({error:'COMPANY_REGISTRY_ERROR',message:e.message});}};
