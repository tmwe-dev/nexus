# Phase 49 — Deployment-aware System Map

Adds `system.map.v2`, deployment synchronization awareness, and a dedicated deployment map view.

The main Home consumes System Map v2 so counters and production cutover status include deployment synchronization.

Original systems remain untouched/read-only.
